# example netlify.toml
[build]
  command = "npm run build"
  functions = "netlify/functions"
  publish = "frontend/build"

[build.environment]
  CI = "false"
  # Frontend site base URL used in generated PDFs and links
  REACT_APP_BASE_URL = "https://computechexamplatform.netlify.app"
  # Optional: set this to your backend API in production (uncomment and set if available)
  # REACT_APP_API_URL = "https://computech-exam-platform.onrender.com"

  ## Uncomment to use this redirect for Single Page Applications like create-react-app.
  ## Not needed for static site generators.
  [[redirects]]
    from = "/*"
    to = "/index.html"
    status = 200

  ## (optional) Settings for Netlify Dev
  ## https://github.com/netlify/cli/blob/main/docs/netlify-dev.md#project-detection
  #[dev]
  #  command = "yarn start" # Command to start your dev server
  #  port = 3000 # Port that the dev server will be listening on
  #  publish = "dist" # Folder with the static content for _redirect file

  ## more info on configuring this file: https://ntl.fyi/file-based-build-config
